# Media Order Configuration

Document package-specific sorting defaults, UI options, upload behavior, and any supported feature flags here.

Keep examples synchronized with the released package version and supported Umbraco versions.
