# Diagnostics

When collecting diagnostics for a UMBFY package, prefer the minimum information needed to reproduce the problem.

Useful details may include package version, Umbraco version, .NET version, environment/topology, relevant package health state, browser console output, and application logs.

Sanitize all diagnostics before sharing publicly.
