# Safe Delete

Safe Delete protects referenced content and media from accidental deletion.

Depending on configuration, Content Guard can warn, require confirmation, or block destructive operations when inbound dependencies exist. Enforcement is authorization-aware and designed to remain fail-safe when dependency state cannot be trusted.
