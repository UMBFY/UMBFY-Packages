# Collaboration Load-Balanced Setup

For multi-node Umbraco deployments, Collaboration must use the supported shared realtime/state configuration for the released package version.

Document Redis/backplane requirements, node consistency, reconnect behavior, and production validation steps here. Do not assume a single-node topology when deploying behind a load balancer.
