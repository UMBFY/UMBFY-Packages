# Collaboration Troubleshooting

Check package/Umbraco versions, SignalR connectivity, authorization, realtime/backplane configuration, browser console output, and application logs.

For load-balanced environments, also verify shared state/backplane connectivity and that all nodes use compatible configuration.
